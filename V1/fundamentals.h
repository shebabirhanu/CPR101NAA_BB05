/* 
[fundamentals.h] : Fundamentals Header File
Purpose: Header File for Converting Demo Function
*/

#ifndef  _FUNDAMENTALS_H   // We declare the header file They are executed only if the conditional succeeds
#define _FUNDAMENTALS_H   // We define  the header file 
#include <stdio.h>       // Standard Input Output in header file 
#include <stdlib.h>     // standard library in header file 
#include <string.h>    

#define TRUE 1 

void fundamentals();   // that's mean this function doesn't return a value

 
#endif                // directive was end 